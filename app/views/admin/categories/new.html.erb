<div class="admin-inner-content">
  <div class="admin-inner-content-title">New Category</div>
  <div class="admin-inner-content-info">
    <%= raw msg_flash(flash) %>
    <%= form_for([:admin, @category], :url => admin_categories_path,
                 :html => { :multipart => true}) do |f| %>
      <%= f.error_messages %>
      <%= render :partial => 'form', :locals => { :f => f} %>
      <div class="field">
        <%= f.submit 'Create Category' %>&nbsp;&nbsp;
        <%= link_to 'Back', admin_categories_path %>
      </div>
    <% end %>
    <div class="clear"></div>
  </div>
</div>
