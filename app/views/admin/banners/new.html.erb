<div class="admin-inner-content">
  <div class="admin-inner-content-title">Banners</div>
  <div class="admin-inner-content-info">
    <%= raw msg_flash(flash) %>
    <%= form_for([:admin, @banner], :url => admin_banners_path,
                 :html => { :multipart => true}) do |f| %>
      <%= f.error_messages %>
      <%= render :partial => 'form', :locals => { :f => f} %>
      <div class="field">
        <%= f.submit 'Create Banner' %>&nbsp;&nbsp;
        <%= link_to 'Back', admin_banners_path %>
      </div>
    <% end %>
    <div class="clear"></div>
  </div>
</div>
