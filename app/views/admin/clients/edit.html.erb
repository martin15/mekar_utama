<div class="admin-inner-content">
  <div class="admin-inner-content-title">Edit Client</div>
  <div class="admin-inner-content-info">
    <%= raw msg_flash(flash) %>
    <%= form_for([:admin, @client], :url => admin_client_path,
                  :html => { :multipart => true, :method => :put }) do |f| %>
      <%= f.error_messages %>
      <div class="admin-category-testimonial" style="height: 225px;">
        <%= image_tag @client.client_image.url(:medium) %>
      </div><br /><br />
      <%= render :partial => 'form', :locals => { :f => f} %>
      <div class="field">
        <%= f.submit 'Update Client' %>&nbsp;&nbsp;
        <%= link_to 'Back', admin_clients_path %>
      </div>
    <% end %>
    <div class="clear"></div>
  </div>
</div>

